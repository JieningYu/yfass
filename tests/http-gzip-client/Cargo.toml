[package]
name = "test-http-gzip-client"
version = "0.1.0"
edition = "2024"

[lints]
workspace = true

[profile.release]
lto = "thin"

[dependencies]
reqwest = { version = "0.12", features = ["blocking"] }
flate2 = "1.1"
